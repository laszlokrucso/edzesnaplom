import test from 'node:test';
import assert from 'node:assert/strict';
import {monthDays,shiftMonth,calendarMonth} from '../lib/calendar.mjs';
test('Monday-first calendar handles leap day and six-row months',()=>{const leap=monthDays('2024-02');assert.equal(leap[3],'2024-02-01');assert.equal(leap.filter(Boolean).length,29);assert.ok(leap.includes('2024-02-29'));const six=monthDays('2026-03');assert.equal(six.length,42);assert.equal(six[6],'2026-03-01');assert.equal(six[36],'2026-03-31');assert.equal(monthDays('2027-02').length,28);});
test('Month navigation crosses year boundaries without skipping February',()=>{assert.equal(shiftMonth('2026-12',1),'2027-01');assert.equal(shiftMonth('2026-01',-1),'2025-12');assert.equal(shiftMonth('2024-03',-1),'2024-02');});
test('Multiple workouts count as one training day; dates use the same UTC date as the diary',()=>{const workouts=[{id:'one',date:Date.parse('2026-09-01T00:00:00Z')},{id:'two',date:Date.parse('2026-09-01T23:30:00Z')},{id:'three',date:Date.parse('2026-09-30T23:59:00Z')},{id:'other',date:Date.parse('2026-10-01T00:00:00Z')}];const original=JSON.stringify(workouts),result=calendarMonth(workouts,'2026-09');assert.equal(result.trainingDays,2);assert.equal(result.workoutCount,3);assert.deepEqual(result.days.get('2026-09-01').map(w=>w.id),['one','two']);assert.equal(result.days.has('2026-10-01'),false);assert.equal(JSON.stringify(workouts),original);assert.equal(calendarMonth([],'2026-09').workoutCount,0);});
